/*=========  DOM ==========
 Document object model = DOM is a tree like structure which will place all HTML elements in a tree.
**we can easily traverse throghu different nodes if we have tree like structure.
** DOM model is reposinsible to add interactivity on the website.
*/



